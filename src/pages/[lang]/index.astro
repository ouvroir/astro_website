---
import {
    KNOWN_LANGUAGE_CODES,
} from '/src/data/languages.ts';

export const getStaticPaths = () => KNOWN_LANGUAGE_CODES.map(lang => ({params: { lang }}))


const { lang } = Astro.params;

---
<meta http-equiv="refresh" content={`0;url=/${lang}/accueil`} />